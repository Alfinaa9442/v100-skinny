# Benchmark/probe harness dependencies (client-side).
# The serving stack (1Cat-vLLM 1.2.2, torch 2.10+cu128) is installed
# separately on the target box; see docs/DEPLOYMENT.md.
torch>=2.10
safetensors>=0.4
numpy>=1.26
